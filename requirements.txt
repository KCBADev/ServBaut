# Dependencias directas — Servicio Bautista
# Probado con Python 3.12.10 en Windows.
#
# Instalación:
#   py -3.12 -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

streamlit>=1.63.0    # interfaz de la aplicación
pandas>=3.0.0        # lectura del Excel de origen y consultas del dashboard
openpyxl>=3.1.5      # motor que pandas usa para leer .xlsx
altair>=6.2.0        # gráficas del dashboard
vl-convert-python>=1.6.0  # exporta a PNG las gráficas de los análisis
# La orden de trabajo se imprime renderizando la plantilla HTML del taller con
# Chromium: el diseño usa flexbox, grid y @page, que ninguna librería de PDF en
# Python interpreta. Tras instalar, hace falta bajar el navegador una vez:
#     .venv\Scripts\python.exe -m playwright install chromium
playwright>=1.40.0

# La base de datos es SQLite, incluida en la librería estándar de Python
# (módulo sqlite3): no requiere instalación.
# El hash de contraseñas usa hashlib.pbkdf2_hmac, también estándar.
